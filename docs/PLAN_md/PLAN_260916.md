# 2026-09-16 计划

## Custom Categories — 自定义分类打标（后端）

- [x] 阅读 custom-categories 功能描述文档与 memgo 现有 add 管线、config、server 结构。
- [x] `core/config`：`Category` 类型、`DefaultCategories()`（内置 15 目录，不默认启用）、`ParseCategories` wire 校验、`MemoryConfig.CustomCategories` 解析。
- [x] `core/prompts/prompts_manual.go`：分类 system prompt + user prompt 拼装（手动文件，不碰 parity 生成文件）。
- [x] `core/memory/categories.go`：目录解析（per-call 非空整体替换 > config 级 > 未配置关闭）、单次 LLM 打标、输出严格解析（缺项/越界/重复/未知分类报错）。
- [x] `core/memory/add.go` 穿线：`AddParams.CustomCategories`、Phase 5.5 打标、payload `category`、响应可选 `category` 键；`infer=false` 与 procedural 不打标。
- [x] `server/api/memories.go`：`POST /memories` 收 per-call `custom_categories`（语义错误 400）；`server/api/configure.go`：`/configure` 校验 config 级字段（400）。
- [x] 单测：memory 4 个（per-call 覆盖断言到分类 prompt 文本 / config 级回落 / 关闭零痕 / 坏输出报错）+ config `ParseCategories` 8 断言；`go build ./...`、`go vet ./...`、`go test ./...` 全绿。
- [ ] 黑盒契约套件未跑（Podman 栈未启动）：默认路径零差异由"关闭零痕"单测锁定，实跑待补验证。
- [x] 移除本功能代码中的来源仓库字样描述（全仓 grep 残留仅 dashboard svg base64 巧合子串，按既定决策不动）。

## Custom Categories — Dashboard 管理页

- [x] categories 页从 LockedPage 假数据 mockup 重写为真实管理页：`GET /configure` 读取、行内编辑 name/description、增删条目、dirty 检测、空列表=关闭、admin 门控（非 admin 只读）。
- [x] 保存走 `POST /configure` 部分更新 `{custom_categories: [...]}`（深合并对数组整体替换）；移除 Cloud/Enterprise 锁定推销文案。
- [x] `pnpm typecheck` 通过；prettier 本文件通过（其余 7 个 warn 为存量基线文件）。
- [ ] **AGENTS 与 REQUIREMENTS 均要求"首次修改 Dashboard 产品代码须在同一变更补齐单测与 Playwright"——本轮未补齐，记为未完成缺口，需用户决策是否本轮搭建测试基建。**
- [ ] 页面实打冒烟未做（server/dashboard 未启动）：保存→回读→add 记忆验证 `category` 待补。

## 文档维护

- [x] `docs/REQUIREMENTS.md` — 新增 Custom Categories 需求条目（行为规则 + 验收标准）。
- [x] `AGENTS.md` — `core/memory` 仓库地图行补分类打标说明。
- [x] 创建本文档。

## Webhooks — 事件通知（后端）

- [x] 阅读功能描述文档（四事件词表、CRUD、payload 结构）与 memgo server/store/core 现有结构。
- [x] 迁移 `007_create_webhooks.sql`（应用库 webhooks 表）+ `server/store/webhooks.go` CRUD。
- [x] core 事件端口：`core/memory/events.go`（`Event` + `EventSink` 接口，core 不做 HTTP；ADD/UPDATE/DELETE/CATEGORIZE 常量）。
- [x] emit 落点：persistRecords（ADD + CATEGORIZE 跟随）、createMemory（infer=false/procedural ADD）、updateMemory（UPDATE 新文本）、deleteMemory（DELETE 删除时当前文本，DeleteAll 逐条）。
- [x] `server/webhook/dispatcher.go`：实现 EventSink，异步投递（10s 超时、非 2xx 报错、失败仅告警）、订阅过滤（空 event_types=全订阅）、`X-Memgo-Event` 头、payload 按文档（记忆事件含 id/data/event，CATEGORIZE 含 categories）。
- [x] AppState/main.go 接线：`NewAppState` 增 sink 参数，config 重建后重新注入。
- [x] `server/api/webhooks.go`：POST/GET/PUT/DELETE /webhooks（admin 增删改、GET 登录可见；url http(s) 校验 400、事件名白名单 400、pydantic 形状 422）+ 路由注册。
- [x] 测试：core events_test 3 个（ADD+CATEGORIZE 序列 / infer=false 仅 ADD / UPDATE+DELETE 文本）；dispatcher_test 4 个（订阅过滤 / payload 形状 / httptest 实打投递+误投递窗口 / 非 2xx 报错）。`go test ./...` 全绿、gofmt/vet 过。
- [ ] 契约套件未跑（Podman 栈未起）；dispatcher 走真库 ListWebhooks 路径未实打（测试用 listHooks 注入）。

## Webhooks — Dashboard 管理页

- [x] webhooks 页从 LockedPage mockup 重写为真实管理页：创建表单（URL/name/四事件复选，默认全选）、列表（badge 事件条目）、行内编辑（Pencil 展开 url/name/事件 + Save/Cancel）、删除；admin 门控，非 admin 只读。
- [x] `api-endpoints.ts` 增 `WEBHOOK_ENDPOINTS`；`pnpm typecheck` 过、prettier 本文件过。
- [ ] 同 Dashboard 单测 + Playwright 既有缺口，仍未补齐（与 categories 页同一决策点）。

## 文档维护

- [x] `docs/REQUIREMENTS.md` — 新增 Webhooks 需求条目。
- [x] `AGENTS.md` — server 地图行补 webhook 投递器说明。
