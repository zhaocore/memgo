# 2026-09-11 工作计划

## Node CLI 重写规划

- [x] 阅读仓库规范、需求、迁移计划及 Node CLI 实现与测试，检查并保留既有未提交内容。
- [x] 写入 Node CLI 重写需求，明确本次授权与上游冻结规则的关系。
- [x] 制定目录、架构、中文注释、文档及 Node 24 的分阶段实施与验收计划。
- [x] 检查文档链接、需求层级和差异，记录验证边界。

规划阶段交付仅为计划；实施步骤见 [Node CLI 重写计划](../node-cli-rewrite-plan.md)，当时尚未开始产品代码改动；后续实施结果见下节。

验证：三个文档的标题层级、相对文件链接、行尾空白检查通过，`git diff --check` 通过。规划阶段未运行产品测试、未宣称 Node 24 已兼容；保留既有 AGENTS.md、CodeGraph 状态及未跟踪文档，不创建提交。

## Node CLI 实施与验收

起点：`6a0127b59caf5bfed699e1033fefe33466bffec2`。已有改动为 AGENTS.md、CodeGraph PID 和未跟踪 docs 文档，均保留。初始环境：Node 24.14.1、pnpm 11.3.0；最终固定 pnpm 11.21.0。

- [x] P0：建立命令及运行基线，明确错误兼容边界。
- [x] P1：修复 Node 24 安装、检查及 CI 工具链。
- [x] P2：拆分 backend、配置、输出和调用状态。
- [x] P3：逐项迁移命令与初始化流程。
- [x] P4：中文注释、用户及维护文档。
- [x] P5：完整测试、安装包、兼容矩阵与终端验收。

初始发现：pnpm 11 冻结安装因 workspace 缺少 packages 失败；原 lint 已有 4 项错误。基线使用已有本地工具补测，未将安装失败视为兼容通过。

验收：完整 Node 测试 14 文件、158 项通过；类型检查、Biome、构建及 PTY 通过。最终安装包在 Node 18.20.8 / 20.19.2 / 22.21.1 / 24.14.1 冒烟通过，完整命令树与基线一致，中文注释审计通过。Go test、vet、build 通过。

- [ ] 补验 Go lint：本机未安装 golangci-lint。
- [ ] 补验服务端契约：compose.contract.yaml 缺失、Podman 未运行；未清理 volume。
- [ ] 补验真实平台账户、邮件送达、代理客户端及远程 CI。

兼容影响、证据及回滚方式见 [重写计划验收结果](../node-cli-rewrite-plan.md#验收结果与边界) 和 [需求](../REQUIREMENTS.md#node-cli-重写)。未提交、未发布，用户既有改动保留。

## Node CLI ESLint 与 JSDoc 规范

- [x] 检查 ESM 导入、工具链及既有规范，保留上轮未提交改动。
- [x] 增加 ESLint flat config，接入 TypeScript 与 JSDoc 校验。
- [x] 修正规则发现的问题，补全中文 JSDoc；说明 `.js` 导入原因。
- [x] 执行 lint、类型检查、完整测试及安装包验收，更新需求与开发文档。

验收结果：Node 24.14.1 / pnpm 11.21.0 冻结安装通过；ESLint 10、TypeScript 推荐规则、JSDoc 规则及 Biome 零错误、零警告，类型检查通过。7 组内存规则探针验证正确中文注释通过，以及缺少文档、参数说明、参数名错配、缺少返回说明、英文摘要、错误标签均被拦截。完整 Node 测试 14 文件、158 项通过，构建及 PTY 通过；隔离安装包在 Node 18.20.8 / 20.19.2 / 22.21.1 / 24.14.1 冒烟通过；完整 Go 测试、vet、build 通过。

变更包括新增 ESLint flat config 和开发依赖、将 ESLint 接入现有 lint/CI 入口、补齐中文 JSDoc、清理检查发现的未使用导入和无效赋值。保留 `.js` 相对导入；没有改变命令或服务端合同。ESLint 不进入生产依赖。回滚时仅撤回本轮规则、依赖、注释及检查清理，不覆盖重写或用户既有改动。远程 CI 未运行；此前 Go lint 工具及外部集成环境缺口保持原状态。未提交、未发布。

## CLI 大号标志

- [x] 核对 LOGO 与品牌输出代码，保留既有改动。
- [x] 将大号字符标志改为用户指定的 MEMGO CLI，检查面板对齐。
- [x] 执行检查与完整测试，记录结果。

验收：终端面板展示 MEMGO CLI，边框对齐；lint、类型检查、构建、158 项 Node 测试及完整 Go 测试通过。未提交。

## CLI 青春明亮配色

- [x] 检查六个颜色常量与现有品牌测试，保留既有改动。
- [x] 更新主色、强调色及语义色，同步已有测试与需求。
- [x] 执行 lint、类型检查、构建和完整测试。

验收：ESLint/JSDoc、Biome、类型检查、构建、158 项 Node 测试及完整 Go 测试通过。仅调整六个品牌色值及已有测试预期；未提交。

## Python CLI 改造

起点：`87fff44`（Node 改造已由用户提交）；仅有 `cli/node/biome.json` 的既有改动，保留。Python 保持现有命令、选项、配置和 Platform API 合同；本次明确授权覆盖原上游冻结规则。

- [ ] P0：建立隔离环境、现有测试与完整命令树基线。
- [ ] P1：拆分入口注册、命令处理、配置、后端与运行状态。
- [ ] P2：整理类型与外部数据校验，补齐中文注释和规范 docstring。
- [ ] P3：完善 Ruff、类型检查、中文文档和实际 CI 检查入口。
- [ ] P4：完整测试、构建、安装包与本地 HTTP/终端验收，记录真实集成缺口。

## Node CLI Logo 文案修正

- [x] 定位 `MOMGO CLI` 拼写及相关品牌测试，保留既有未提交改动。
- [x] 将大号字符标志、需求真源和测试改为 `MEMGO CLI`。
- [x] 运行 Node 全量测试、类型检查、lint、构建以及文档和差异检查：159 项测试通过。

## Python 大号 Logo

- [x] 核对截图对应的 Python 品牌模块及当前工作区。
- [x] 将字符画改为用户最新指定的 `MEMGO LI`，同步需求。
- [x] 80 列终端面板显示完整、边框对齐；Python 完整测试 199 项通过，品牌模块 Ruff 检查通过。
