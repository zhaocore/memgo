# 2026-09-15 计划

## Graph Memory — 内存图索引与多跳遍历

- [x] 阅读 repo 规范、需求、已有计划、受影响目录的测试; 检查并保留既有未提交改动。
- [x] 创建 `core/graph/graph.go` — `GraphIndex` 双邻接表 + BFS 多跳遍历 + 增量回调维护。
- [x] 新增 `GraphMemoryConfig` 到 `core/config/config.go`（`enable_multi_hop`、`max_hops`、`decay_factor`）。
- [x] `entity.Store` 注入 `*graph.GraphIndex`，Upsert/Remove 回调同步，新增 `BuildIndex`、`ComputeGraphBoosts` 方法。
- [x] `memory.Memory` 注入 `graphIndex` + `graphCfg`，新增 `BuildGraphIndex()` 启动时构建方法。
- [x] `memory/search.go` 搜索流水线新增 Step 6.5 多跳扩展，多跳记忆补充入候选集。
- [x] 创建 `core/graph/graph_test.go` — 7 个测试（Build、MultiHop、Upsert、Remove、MaxHops、Empty、MemoryBoosts），全部通过。
- [x] `go test ./...`、`go vet ./...`、`go build ./...` 全部通过。

## DisableDocs — Swagger/ReDoc/OpenAPI 可关闭

- [x] `server/api/server.go` `Config` 新增 `DisableDocs bool`，routes 条件注册 + `redirectDocs` 判关。
- [x] `cmd/server/main.go` 通过 `envTrue("DISABLE_DOCS")` 注入。

## 文档维护

- [x] `AGENTS.md` — 补充 `core/graph/` 包地图项、`config` 节点图记忆说明、DisableDocs 文档开关记录。
- [x] `AGENTS.md` — 移除已删文件 `docs/go-refactor-plan.md` 的引用（步骤 1）。
- [x] 创建本文档。
- [x] `docs/REQUIREMENTS.md` — 新增 Graph Memory 与 DisableDocs 需求条目。